 

# **Ejercicio 1**

## **¿Qué comando utilizaste en el paso 11? ¿Por qué?**
### Utilice ```git reset --hard HEAD~1```
## **¿Qué comando o comandos utilizaste en el paso 12? ¿Por qué?**
### Utilice ```git reflog```, busque el codigo del último commit y ejecute el comando ```git reset --hard <code>``` para rehacer el commit
## **El merge del paso 13, ¿Causó algún conflicto? ¿Por qué?**
### Al parecer la rama ya estaba mergeada, este puede pasar debido que al hacer el merge con master, y styled absorber master, este se puede haber quedado con el último commit hijo, es por esto que no se refleja algún cambio al momento de hacerle ```cat``` a git-nuestro.md
## **El merge del paso 19, ¿Causó algún conflicto? ¿Por qué?**
### Hubo un conflicto porque las ramas styled y htmlify al tener modificaciones cada una y diferentes commits provocaron un conflicto, el cual fue resuelto eligiendo con qué versión de **git-nuestro.md** quedarse y a la vez, absorbiendo los commits y contenido de styled
## **El merge del paso 21, ¿Causó algún conflicto? ¿Por qué?**
### Utilice el comando ```git merge <branch>```, no hubo ningún problema porque el contenido de master había sido previamente absorbido por styled, el cual también fue absorbido por htmlify
## ¿Qué comando o comandos utilizaste en el paso 25?
### Utilice ```git log --graph``` para dibujar el diagrama
## **El merge del paso 26, ¿Podría ser fast forward? ¿Por qué?**
### Si, tambien podria ser fast forward, ya que en este caso, no nos daria ningun problema en la construccion de el archivo git-nuestro.md
## **¿Qué comando o comandos utilizaste en el paso 27?**
### Para deshacer el merge sin perder los cambios del working copy use el comando ```git reset HEAD~1```
## **¿Qué comando o comandos utilizaste en el paso 28?**
### Descarte los cambios regresando al merge entre master y styled con el comando ```git reset --hard <code>```
## **¿Qué comando o comandos utilizaste en el paso 29?**
### Utilice el comando ```git branch -D <branch>``` para borrar la rama title
## **¿Qué comando o comandos utilizaste en el paso 30?**
### Utilice git reflog para encontrar el código del merge entre master y title, use el comando ```git reset --hard <code>``` para rehacer el merge
## **¿Qué comando o comandos usaste en el paso 32?**
### Utilice ```git reflog``` para encontrar el código del commit inicial y también ```git checkout <code>``` para regresar al commit
## **¿Qué comando o comandos usaste en el punto 33?**
### Utilice ```git reflog``` para encontrar el código del commit final y también ```git checkout <code>``` para regresar al commit


